<% content_for :sidebar do %>
  <div data-hook="homepage_sidebar_navigation">
    <% if "spree/products" == params[:controller] && @taxon %>
      <%= render :partial => 'spree/shared/filters' %>
    <% else %>
      <%= render :partial => 'spree/shared/taxonomies' %>
    <% end %>
  </div>
<% end %>

<div id="content_oc">          
    <div class="box_oc">              
        <div class="box-heading">
            <h1 class="mijoshop_heading_h1"><%= t(:our_partners) %></h1>
        </div>
        <div class="box-content">
        	<% @partners.each do |partner|%>
               <div class="row div-logo">
	                <div class="span2"><%= link_to(image_tag(partner.icon.url(:normal)), partner_path(partner.id)) %></div>
	                <div class="span10">
	                    <h4><%= link_to partner.name, partner_path(partner.id) %></a></h4>
	                    <p><%= raw(partner.description) %></p>
	                </div>
               </div>
            <% end %>   
        </div>               
    </div>
</div>

